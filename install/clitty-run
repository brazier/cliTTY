#!/usr/bin/env bash
set -euo pipefail

INSTALL_DIR="/opt/clitty"
VENV_DIR="$INSTALL_DIR/.venv"

if [[ ! -d "$VENV_DIR" ]]; then
    echo "Error: clitty is not installed. Run clitty-install.sh first." >&2
    exit 1
fi

source "$VENV_DIR/bin/activate"
python "$INSTALL_DIR/main.py" "$@"
